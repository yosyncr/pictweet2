<div class="contents row">
  <div class="container">
    <h3>編集する</h3>
    <%= form_with(model: @tweet, local: true) do |form| %>
      <%= form.file_field image %>
      <%= form.text_area :text, placeholder: "text", rows: "1" %>
      <%= form.submit "SEND" %>
    <% end %>
  </div>
</div>
